/*const breath = document.getElementById("breath")
breath.style.position = "absolute" ; 
let pagePos = 0
let dir = 1

function breathing() {
    if (pagePos == 10) {
        dir = -0.25 
    }
    else if (pagePos == 0) {
        dir = 0.25
    }
    pagePos += 0.5 * dir;
    breath.style.top = pagePos+"px";
    requestAnimationFrame(breathing)
}
*/
